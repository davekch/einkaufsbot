[project]
name = "einkaufsbot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "python-telegram-bot[job-queue]==20.7",
    "pyyaml>=6.0.2",
]

[dependency-groups]
dev = [
    "telethon>=1.39.0",
]
